meta {
  name: Recordings - create (POST)
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/entities/recordings
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "title": "Midnight Session Take 1",
    "durationSeconds": 243,
    "recordedOn": "2026-03-14",
    "isLive": false,
    "trackNumber": 1
  }
}

assert {
  res.status: eq 200
  res.body.iri: isDefined
}

script:post-response {
  bru.setVar("recordingIri", res.body.iri);
}
