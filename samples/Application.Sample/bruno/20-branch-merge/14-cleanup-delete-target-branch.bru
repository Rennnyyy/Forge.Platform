meta {
  name: Branch merge - cleanup delete target branch (DELETE)
  type: http
  seq: 14
}

delete {
  url: {{baseUrl}}/api/branches?iri={{mergeTgtIri}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
}
