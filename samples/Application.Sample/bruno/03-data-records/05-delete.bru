meta {
  name: DataRecord - delete (POST)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/entities/data-records/delete
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "iri": "{{dataRecordIri}}"
  }
}

assert {
  res.status: eq 200
}
