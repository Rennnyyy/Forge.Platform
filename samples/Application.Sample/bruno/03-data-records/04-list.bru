meta {
  name: DataRecord - list (GET)
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/entities/data-records
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.items: isDefined
}
