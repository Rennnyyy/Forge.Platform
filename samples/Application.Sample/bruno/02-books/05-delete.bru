meta {
  name: Books - delete (POST)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/entities/books/delete
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "iri": "{{bookIri}}"
  }
}

assert {
  res.status: eq 200
}
