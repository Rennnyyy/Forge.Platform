meta {
  name: Branches - delete branch (DELETE ?iri=)
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/api/branches?iri={{branchIri}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
}
