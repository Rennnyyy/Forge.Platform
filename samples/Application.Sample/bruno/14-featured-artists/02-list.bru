meta {
  name: Featured Artists - list (GET)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/entities/featured-artists
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.items: isDefined
}
