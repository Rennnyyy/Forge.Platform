meta {
  name: Snapshots - list snapshots only (GET /api/snapshots)
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/snapshots
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.items: isDefined
}
