meta {
  name: Snapshots - list all branches (GET, includes source branch)
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/branches
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.items: isDefined
}
