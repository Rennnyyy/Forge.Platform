meta {
  name: Studios - delete (DELETE ?iri=)
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/api/entities/studios?iri={{studioIri}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
}
