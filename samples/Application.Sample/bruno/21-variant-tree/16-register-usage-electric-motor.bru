meta {
  name: Structure tree - register usage EV Powertrain → Electric Motor (unconditional)
  type: http
  seq: 16
}

post {
  url: {{baseUrl}}/api/entities/structure-usages
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "parentStructureIri": "{{evPowertrainIri}}",
    "childStructureIri": "{{electricMotorIri}}"
  }
}

assert {
  res.status: eq 200
  res.body.iri: isDefined
}
