meta {
  name: Genres - read Jazz by IRI (GET ?iri=)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/entities/genres?iri=https://forge-it.net/genres/jazz
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.iri: isDefined
  res.body.slug: eq jazz
  res.body.name: eq Jazz
}
