meta {
  name: Artists - read (GET ?iri=)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/entities/artists?iri={{artistIri}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.iri: isDefined
  res.body.name: eq Aria Nova
  res.body.active: eq true
}
