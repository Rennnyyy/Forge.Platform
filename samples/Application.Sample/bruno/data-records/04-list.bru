meta {
  name: DataRecord - list (POST)
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/entities/data-records/list
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {}
}

assert {
  res.status: eq 200
  res.body.items: isDefined
}
