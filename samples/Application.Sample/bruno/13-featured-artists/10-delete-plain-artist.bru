meta {
  name: Featured Artists - delete plain Artist (cleanup)
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/api/entities/artists?iri={{plainArtistIri}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
}
