meta {
  name: Branch aspect demo - list all branches (GET)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/branches
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.items: isDefined
}
